/**
 * @file
 * barley_boyos behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.barleyBoyos = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
